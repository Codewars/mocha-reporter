const assert = require("assert");

describe("group 1", () => {
  it("test 1", () => {
    assert.strictEqual(1, 1);
  });
});

describe("group 2", () => {
  it("test 1", () => {
    assert.strictEqual(1, 1);
  });
});
